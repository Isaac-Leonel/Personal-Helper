import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class HorarioAlerta extends StatelessWidget{
  const HorarioAlerta({Key? key, required this.alertName, required this.levelWarning, required this.alertIcon, required this.alertTime}) : super(key: key);

  final String alertName;
  final String levelWarning;
  final String alertIcon;
  final String alertTime;

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }

}